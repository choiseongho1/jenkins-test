name: Trigger ReviewBot

on:
  pull_request:
    branches:
      - main

jobs:
  trigger-reviewbot:
    runs-on: ubuntu-latest
    steps:
      - name: Call ReviewBot Webhook
        run: |
          curl -X POST "http://aireviewbot.kr/api/webhook" \
               -H "Content-Type: application/json" \
               -d '${{ toJson(github.event) }}'
